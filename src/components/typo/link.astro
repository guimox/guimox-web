---
const { aria, target, href } = Astro.props
---

<a
  aria-label={aria}
  target={target}
  href={href}
  class="inline-flex w-fit cursor-pointer items-center gap-1.5 text-base hover:opacity-75 md:text-lg"><slot /></a
>
