<span class="highlight">
  <slot />
</span>

<style>
  .highlight {
    position: relative;
    display: inline-block;
    box-shadow: inset 0 -0.6em 0 var(--highlight-shadow-color);
  }

  .highlight::before {
    content: '';
    position: absolute;
    bottom: 0.2em;
    left: 0;
    right: 0;
    height: 40%;
    z-index: -1;
  }

  :root {
    --highlight-shadow-color: theme('colors.color-light');
  }

  @media (prefers-color-scheme: dark) {
    :root {
      --highlight-shadow-color: theme('colors.color-dark');
      --highlight-bg-color: theme('colors.color-dark');
    }
  }

  /* For manual dark mode toggle */
  :root.dark {
    --highlight-shadow-color: theme('colors.color-dark');
    --highlight-bg-color: theme('colors.color-dark');
  }
</style>
